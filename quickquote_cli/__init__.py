"""QuickQuote CLI — DOCX/PDF generator spawned as a subprocess by the Electron
main process. See cli.py for the input/output JSON shapes.

Mirror of QuickProp v3.1.1's quickprop/ generator modules, packaged for use
out-of-process. The Python code itself is unchanged from QuickProp; only
paths.py is adapted to the new layout.
"""
